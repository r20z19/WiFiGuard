# WIFIGuard

<p align="center">
  <strong>智能无线网络安全防护与入侵检测系统</strong>
</p>

<p align="center">
  集 <strong>实时监控 · 入侵检测 · 主动防御 · 可视化仪表盘 · 邮件告警</strong> 于一体
</p>

---

## 📖 目录

- [项目简介](#项目简介)
- [核心功能](#核心功能)
- [快速开始](#快速开始)
- [用户指南](#用户指南)
  - [登录系统](#登录系统)
  - [系统概览](#系统概览)
  - [当前告警](#当前告警)
  - [历史告警](#历史告警)
  - [在线设备](#在线设备)
  - [白名单管理](#白名单管理)
  - [黑名单管理](#黑名单管理)
  - [邮箱推送](#邮箱推送)
  - [网络拓扑](#网络拓扑)
  - [AI 功能](#ai-功能)
  - [安全报告](#安全报告)
  - [系统日志](#系统日志)
- [运行模式](#运行模式)
- [配置参考](#配置参考)
- [常见问题](#常见问题)
- [技术架构](#技术架构)

---

## 项目简介

WIFIGuard 是一套全栈 WiFi 安全防护系统，能够**实时检测 8 种无线网络攻击**，并提供**自动防御**能力。无需任何云服务，全部在本地运行，保护您的数据隐私。

### 它能做什么？

| 能力 | 说明 |
|------|------|
| 🔍 **实时检测** | 自动识别 Deauth 攻击、钓鱼 AP、暴力破解、泛洪攻击等 8 种威胁 |
| 🛡 **主动防御** | 检测到攻击后自动发送 Deauth 帧将攻击者踢出网络 |
| 📊 **可视化监控** | Web 仪表盘直观展示设备状态、攻击趋势、网络拓扑 |
| 📧 **实时通知** | 攻击发生时通过邮件即时推送告警 |
| 🏠 **全栈本地** | 不依赖任何云服务，数据完全留在您的设备上 |

### 适用场景

- 🏠 **家庭用户**：监控家中 WiFi 是否有陌生设备入侵
- 🏢 **小型办公室**：保护办公网络不被攻击者嗅探或劫持
- 🎓 **教学研究**：学习无线网络安全，理解各类攻击原理
- 🔬 **安全测试**：在授权范围内进行 WiFi 安全评估

---

## 核心功能

### 攻击检测能力（8 种）

| 攻击类型 | 检测原理 | 严重程度 |
|----------|----------|:--------:|
| Deauth 取消认证攻击 | 监控伪造的管理帧，检测强制断连行为 | 🔴 严重 |
| Evil Twin 钓鱼 AP | 7 维度 BSSID 交叉比对，识别伪造同名 WiFi | 🔴 严重 |
| Flood 泛洪攻击 | 统计 Probe/管理帧速率，识别异常流量洪峰 | 🟠 高危 |
| 暴力破解 | 追踪 EAPOL 握手重传模式，发现密码穷举尝试 | 🟠 高危 |
| KRACK 密钥重装 | 检测 WPA2 四次握手中的密钥重装异常 | 🔴 严重 |
| 非法接入 | 监控未授权 MAC 地址的关联请求 | 🟡 中危 |
| 弱加密协议 | 识别 WEP/TKIP 等过时加密方式 | 🟡 中危 |
| 弱密码 | 匹配 20+ 常见默认 SSID 密码模式 | 🟢 低危 |

### 防御能力

| 策略 | 行为 |
|------|------|
| **Monitor 模式** | 仅记录告警，不干预网络 |
| **Blacklist 黑名单** | 检测到黑名单设备后自动发送 Deauth 帧将其断开 |
| **Whitelist 白名单** | 仅允许白名单内设备连接，非白名单设备自动阻断 |

> ⚠️ 黑名单和白名单**互斥**，同一时间只能启用其中一种。

---

## 快速开始

### 环境要求

| 依赖 | 最低版本 | 用途 |
|------|:------:|------|
| Python | 3.10+ | 后端服务 |
| Node.js | 16.0+ | 前端构建 |
| npm | 8.0+ | 包管理 |
| 无线网卡（可选） | 支持 Monitor 模式 | 真实环境监听 |

### 5 分钟快速体验（模拟模式）

模拟模式**不需要无线网卡**，系统会自动生成虚拟设备和攻击数据，适合快速体验功能。

```bash
# 1. 安装 Python 依赖
cd backend
pip install -r requirements.txt

# 2. 安装前端依赖
cd ../frontend
npm install
npm run build

# 3. 启动系统
cd ../backend
python app.py
```

启动后打开浏览器访问 **http://localhost:8000**

- 默认用户名：`admin`
- 默认密码：`admin`

首次登录后系统会要求修改密码。

### 生产部署（真实监听模式）

需要一张支持 Monitor 模式的无线网卡。

```bash
# 1. 将网卡切换为监听模式
sudo ./backend/scripts/setup_monitor.sh wlan1

# 2. 设置环境变量并启动
export WIFIGUARD_SIM=false
export WIFIGUARD_IFACE=wlan1
cd backend && python app.py
```

---

## 用户指南

### 登录系统

系统启动后，浏览器打开 `http://localhost:8000` 进入登录页面。

- 输入用户名和密码，点击"登录"
- 首次登录会弹出修改密码对话框，**必须修改默认密码**后才能使用系统
- 密码长度至少 6 位

> 💡 忘记密码？删除 `backend/data/wifiguard.db` 数据库文件后重启，系统会重置为默认账户。

---

### 系统概览

登录后首先看到的是系统概览页面（Dashboard），它是一站式的网络状态总览。

**状态卡片区（顶部 4 张卡片）：**

| 卡片 | 显示内容 | 点击可跳转 |
|------|---------|:--------:|
| 系统状态 | 当前运行状态 + 运行时长 | 在线设备页 |
| 当前告警 | 告警总数 + 高危/中危分类 | 告警页 |
| 在线设备 | 设备总数 + 正常/可疑分类 | 设备页 |
| 今日告警 | 今日新增 + 历史总计 | 告警页 |

**攻击类型柱状图 + 饼图：**
- 柱状图直观对比各攻击类型数量
- 饼图展示百分比分布
- **点击图表**可跳转到告警页面并自动筛选该类型

**安全建议卡：**
- 根据当前告警情况自动生成分级的处置建议
- 每条建议附带操作按钮，点击即可跳转到对应功能页

**在线设备快速浏览：**
- 展示最近 8 台在线设备
- 显示设备图标（📱手机 / 🖥电脑 / 📡路由器）+ MAC + IP + 状态

---

### 当前告警

告警页面提供攻击事件的集中管理和快速处置。

**筛选工具栏：**
- 严重等级快捷筛选：全部 / 严重 / 高危 / 中危 / 低危
- 攻击类型下拉筛选

**攻击源聚合视图：**
- 按攻击源 MAC 合并显示，一眼看出"谁在攻击"
- 每个攻击源显示攻击类型标签 + 次数 + 最高严重等级

**批量操作：**
- 勾选多条告警 → 点击"批量清除"或"批量加黑名单"
- 一键处置：将攻击源 MAC 加入黑名单并同时清除告警

**右侧详情面板 + AI 解读：**
- 点击"建议"→ 右侧滑出详情面板，显示完整告警信息 + 设备厂商关联
- 自动调用 AI 解读（如已配置），将技术告警翻译为通俗语言
- 📡 可展开查看发送给 AI 的报文上下文数据
- 操作按钮：一键处置(拉黑+清除) / 加黑名单 / 忽略

---

### 历史告警

历史告警页面用于回溯和导出过去的攻击记录。

**筛选工具：**
- **日期快捷按钮**：今天 / 本周 / 本月
- 日期范围选择器
- 攻击类型 + 处理状态筛选

**统计摘要：**
- 表格上方显示当前筛选结果中各严重等级的数量

**数据导出：**
- 点击"📥 导出CSV"按钮，一键下载当前筛选结果为 CSV 文件

**分页浏览：**
- 底部支持翻页，可选择每页显示条数

---

### 在线设备

展示当前连接 WiFi 的所有设备，并提供风险评估。

**设备表格：**

| 列 | 说明 |
|------|------|
| 类型 | 📱手机 / 🖥电脑 / 📡路由器，根据厂商 OUI 自动识别 |
| MAC 地址 | 设备唯一标识 |
| 厂商 | 根据 MAC OUI 数据库自动查询 |
| IP 地址 | 设备当前 IP |
| 连接 SSID | 设备连接的 WiFi 名称 |
| 信号强度 | 进度条 + dBm 数值，颜色区分强/中/弱 |
| **风险评估** | 0-100 分，综合信号、加密、状态、名单计算 |
| 在线时长 | 从首次发现到现在的持续时间 |
| 状态 | 正常 / 可疑 |

**设备详情抽屉：**
- 点击任意设备行 → 右侧滑出详情面板
- 包含：设备标识 + AI 智能识别、网络信息、安全评估（圆形评分 + 风险因子分析）、设备标签（新设备/常驻/信号异常）、行为时间线、快捷操作

**添加黑白名单：**
- ⚠️ 只有开启了对应名单模式才能添加
- 按钮禁用时悬停可看到提示

---

### 白名单管理

白名单用于标记**可信设备**。启用白名单模式后，非白名单设备将被自动阻断。

**使用流程：**
1. 打开"启用白名单"开关
2. 从在线设备批量导入（弹窗勾选）或手动输入 MAC 地址
3. 白名单设备将不会触发安全告警，非白名单设备会被自动断开

**功能特性：**
- 从在线设备批量导入（一键勾选多台设备）
- 手动添加/编辑/移除设备
- MAC 地址自动格式化为大写 + 冒号分隔
- 实时显示白名单设备是否当前在线

> ⚠️ 启用白名单会**自动关闭**黑名单模式（两者互斥）

---

### 黑名单管理

黑名单用于标记**已知攻击设备**。启用黑名单模式后，黑名单设备一旦检测到就会被自动踢出网络。

**使用流程：**
1. 打开"启用黑名单"开关
2. 从在线设备批量导入可疑设备，或手动添加
3. 被加入黑名单的设备再次连接时，系统会自动发送 Deauth 帧将其断开

**功能特性：**
- 从在线设备批量导入
- 加入原因记录
- 关联告警统计（"该设备触发了 X 次告警"）
- 实时显示黑名单设备是否在线（⚠在线标记）

> ⚠️ 启用黑名单会**自动关闭**白名单模式（两者互斥）

---

### 邮箱推送

配置邮箱推送后，系统检测到攻击时会自动向指定邮箱发送告警邮件。

**配置步骤（以 QQ 邮箱为例）：**
1. 登录 QQ 邮箱网页版 → 设置 → 账户
2. 开启 IMAP/SMTP 服务 → 获取授权码
3. 在 WIFIGuard 邮箱推送页面填写：
   - SMTP 服务器：`smtp.qq.com`
   - SMTP 端口：`465`
   - 发件邮箱：你的 QQ 邮箱地址
   - 授权码：第 2 步获取的授权码
   - 收件邮箱：接收告警的邮箱地址
4. 点击"测试连接"验证配置是否正确
5. 打开"启用推送"开关

**支持邮箱：** QQ 邮箱 · 163 邮箱 · Gmail（配置指南已内置在页面中）

---

### 网络拓扑

网络拓扑页面以**室内房间平面图**的形式直观展示 WiFi 网络状态。

**布局说明：**

| 位置 | 元素 | 说明 |
|------|------|------|
| 🏠 房间中央 | 路由器/AP | 绿色大图标 + MAC 地址，持续扩散信号波纹 |
| 🔵 房间内 | 用户设备 | PC（蓝色）/ 手机（青色）卡片，显示 MAC + 信号 + 距 AP 距离 |
| 🔴 房间外 | 攻击者 | 红色卡片 + 闪烁光晕，标注攻击状态 |

**交互操作：**
- **悬停设备卡片** → 弹出信息浮层（MAC / IP / 信号 / 距离）
- **点击设备卡片** → 右侧展示完整详情 + 关联告警列表
- **左侧面板** → 设备列表快速定位
- **攻击连线** → 攻击者到受害设备的红色闪烁虚线 + 箭头，1s 周期

**状态徽章：**

| 徽章 | 含义 |
|------|------|
| ⚡ 攻击中 | 该设备正在发起攻击 |
| 🎯 被攻击 | 该设备是攻击目标 |
| 🚫 已踢出 | 黑名单设备已被断开 |
| 🔴 数字 | 右上角红色圆形，显示受到的攻击次数 |

---

### AI 功能

WIFIGuard 集成了 AI 智能分析能力，支持 **DeepSeek**、**通义千问 (Qwen)**、**智谱 GLM** 三种平台。

**配置方式：**
1. 进入侧边栏「AI 功能」页面
2. 选择 AI 平台，填入 API Key（需自行从平台获取）
3. 点击「测试连接」验证，然后「保存配置」
4. 启用后右下角出现 🤖 浮动聊天按钮

**六大 AI 功能：**

| 功能 | 入口 | 说明 |
|------|------|------|
| 🔍 **智能告警解读** | 告警页 → 点击"建议" → 自动 AI 解读 | 将技术告警翻译为通俗语言，说明攻击原理、影响和处置建议。同时附带报文上下文分析 |
| 💬 **AI 安全顾问** | 右下角 🤖 浮动按钮 | 自然语言对话，询问网络安全问题，AI 根据当前系统状态给出个性化建议 |
| 📊 **自动安全报告** | 安全报告页 → 🤖 AI 生成报告 | 综合当前系统数据，自动生成专业安全报告（总体评估+威胁+建议） |
| 📱 **智能设备识别** | 设备详情抽屉 → 🤖 AI 识别 | 根据 MAC 厂商、信号、连接行为推断设备具体型号 |
| 🔍 **异常行为检测** | 安全报告页 → 执行扫描 | 结合攻击数据+设备行为，AI 逐一分析每个设备是否存在异常 |
| 🔮 **攻击预测** | 安全报告页 → 生成预测 | 基于当前攻击态势，预测未来可能的威胁并给出防范建议 |

**AI 解读中的报文分析：**
- 系统内置网络事件缓冲区（500 条），记录攻击事件和设备活动
- 点击 📡 查看报文上下文 可看到发送给 AI 的事件数据
- AI 综合分析告警+事件日志，给出更精准的解读

---

### 安全报告

安全报告页面是网络安全状态的综合仪表盘。

**报告内容：**
- **综合安全评分**（0-100 环形图）：根据告警数、严重程度、可疑设备综合计算
- **本周安全趋势**：折线图展示攻击频率变化
- **攻击类型分布**：饼图展示各类攻击占比
- **高危设备列表**：自动列出风险评分最高的设备及原因
- **最近攻击事件**：最近 10 条攻击记录

**AI 增强：**
- 🤖 生成报告：AI 根据当前数据生成自然语言安全分析
- 🔍 异常检测：AI 分析每个设备是否存在行为异常
- 🔮 攻击预测：AI 预测未来威胁及风险等级，附带详细理由

---

### 系统日志

系统自动记录所有关键事件，方便审计和排错。

**记录的事件：**
- 攻击检测（检测到什么攻击、源/目标 MAC、严重等级）
- 名单变更（设备加入/移出白名单或黑名单）
- 设备踢出（黑名单强制断开连接）
- 告警处理（清除告警）
- 邮件发送（成功/失败）
- 系统启动/异常

**日志页面功能：**
- 等级筛选（ERROR / WARNING / INFO）+ 类别筛选（攻击/系统/设备/配置）
- 关键词搜索
- 📥 导出 CSV（含 UTF-8 BOM，Excel 可直接打开）

---

## 运行模式

### 模拟模式

**默认模式，无需无线网卡。** 系统内置虚拟数据生成器，自动模拟 8 台设备和多种攻击。

```bash
python app.py  # 默认即为模拟模式
```

适用场景：功能体验、开发调试、演示展示。

### 真实监听模式

需要一张支持 **Monitor 模式**的 USB 无线网卡。

```bash
# 1. 切换网卡为 monitor 模式
sudo ./backend/scripts/setup_monitor.sh wlan1

# 2. 启动监听
export WIFIGUARD_SIM=false
export WIFIGUARD_IFACE=wlan1
python app.py
```

适用场景：生产部署、真实环境安全监控。

### PCAP 回放模式

从预录制的 `.cap`/`.pcap` 文件中离线分析。

```bash
WIFIGUARD_PCAP=test/testcase/global-01.cap python app.py
```

适用场景：事后取证、历史数据分析、检测器调试。

---

## 配置参考

### 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `WIFIGUARD_DB` | `backend/data/wifiguard.db` | SQLite 数据库路径 |
| `WIFIGUARD_IFACE` | `wlan1mon` | 监听网卡接口名称 |
| `WIFIGUARD_SIM` | `true` | 模拟模式开关 |
| `WIFIGUARD_PCAP` | (空) | PCAP 文件路径（逗号分隔多个） |
| `WIFIGUARD_INTERVAL` | `2` | 检测引擎轮询间隔（秒） |
| `WIFIGUARD_SMTP_HOST` | `smtp.qq.com` | SMTP 服务器地址 |
| `WIFIGUARD_SMTP_PORT` | `465` | SMTP 端口 |
| `WIFIGUARD_DEBUG` | `false` | Flask 调试模式 |

### 默认账户

| 字段 | 值 |
|------|------|
| 用户名 | `admin` |
| 初始密码 | `admin` |

---

## 常见问题

### Q: 启动后浏览器显示白屏？
A: 请确认已执行 `npm run build` 构建前端。如果使用开发模式，需要先执行 `cd frontend && npm run dev`，然后访问 `http://localhost:3000`。

### Q: 忘记管理员密码怎么办？
A: 删除 `backend/data/wifiguard.db` 文件后重启系统，账户会重置为 `admin / admin`。

### Q: 邮箱测试连接失败？
A: 常见原因：
- QQ 邮箱需要使用**授权码**而非邮箱密码
- 163 邮箱需要开启 SMTP 服务
- Gmail 需要开启两步验证并生成应用专用密码

### Q: 白名单和黑名单可以同时启用吗？
A: 不可以，两者互斥。启用一个会自动关闭另一个。

### Q: 真实监听模式下看不到设备？
A: 请确认：
1. 无线网卡已正确切换为 Monitor 模式（`iw dev wlan1 info \| grep type`）
2. `WIFIGUARD_IFACE` 环境变量设置正确
3. 所在环境有活跃的 WiFi 网络

### Q: 如何录制 PCAP 文件用于回放？
A: 使用 airodump-ng 或 tcpdump：
```bash
sudo airodump-ng --output-format pcap -w capture wlan1mon
```

### Q: 支持哪些操作系统？
A: 后端支持 Linux（Ubuntu/Debian/Arch/Raspberry Pi OS）。前端可在任何支持 Node.js 的系统上构建。Windows 用户建议使用 WSL2。

---

## 技术架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端展示层                            │
│   Vue 3 + Element Plus + ECharts + Pinia                │
│   仪表盘 · 告警 · 设备 · 黑白名单 · 邮箱 · 拓扑 · 日志  │
│   安全报告 · AI 配置                                     │
├─────────────────────────────────────────────────────────┤
│                    AI 智能层                             │
│   DeepSeek / 通义千问 / 智谱 GLM                        │
│   告警解读 · 安全顾问 · 报告生成 · 异常检测 · 攻击预测   │
├─────────────────────────────────────────────────────────┤
│                    API 网关层                            │
│   Flask RESTful API (11 个路由模块)                      │
├─────────────────────────────────────────────────────────┤
│                    业务逻辑层                            │
│   告警处理 · 访问控制 · 邮箱推送 · 设备管理 · 用户认证    │
│   日志记录 · 报文事件缓冲                                 │
├─────────────────────────────────────────────────────────┤
│                    检测引擎层                            │
│   8 个独立检测器 · 插件式架构 · 统一 analyze() 接口      │
├─────────────────────────────────────────────────────────┤
│                    数据采集层                            │
│   真实监听 (Scapy) · 模拟仿真 · PCAP 回放                │
├─────────────────────────────────────────────────────────┤
│                    数据存储层                            │
│   SQLite (WAL 模式) — 10 张业务表                       │
└─────────────────────────────────────────────────────────┘
```

**技术栈：**
- 前端：Vue 3 (Composition API) + Vite 5 + Element Plus + Pinia + ECharts + Axios
- 后端：Python Flask + Scapy + aircrack-ng
- AI：DeepSeek / Qwen / GLM（用户自备 API Key）
- 数据库：SQLite（10 张表：用户、当前告警、历史告警、在线设备、白名单、黑名单、邮箱配置、推送记录、系统日志、AI 配置）
- 构建：npm + pip

---

## 许可证

本项目仅限**教育、研究和个人使用**。不得用于任何未经授权的网络攻击或非法活动。使用者需遵守当地法律法规，对自己使用本工具的行为负责。

---

<p align="center">
  Made with ❤️ by WIFIGuard Team
</p>
